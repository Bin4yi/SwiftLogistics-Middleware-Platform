spring:
  application:
    name: driver-service
  cloud:
    config:
      uri: http://localhost:8888
      username: config
      password: server123
      fail-fast: false

---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888